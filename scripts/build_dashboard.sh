#!/usr/bin/env bash

#BUCKET=jbenn-roam-backups
#KEY=`aws --profile jason s3 ls ${BUCKET}/backups --recursive | sort | head -n 1 | awk '{print $4}'`
#aws --profile jason s3 cp s3://${BUCKET}/${KEY} data/roam-backup.zip
